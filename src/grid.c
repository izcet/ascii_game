#include "shared.h"

static int	on_list(char c)
{
	char	*list = "\n\t\r"
}

int			check_file(char	*file)
{
	int		row;
	int		col;

	row = 0;
	while (row < MAP_SIZE)
	{
			col = 0;
			while (col < MAP_SIZE)
			{

